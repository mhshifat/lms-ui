import React, { Fragment } from "react";
import { UsersContainer } from "../containers";

const Users = () => {
  return (
    <Fragment>
      <UsersContainer />
    </Fragment>
  );
};

export default Users;
