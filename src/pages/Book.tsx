import React, { Fragment } from "react";
import { BookContainer } from "../containers";

const Book = () => {
  return (
    <Fragment>
      <BookContainer />
    </Fragment>
  );
};

export default Book;
