import React, { Fragment } from "react";
import { UserContainer } from "../containers";

const User = () => {
  return (
    <Fragment>
      <UserContainer />
    </Fragment>
  );
};

export default User;
