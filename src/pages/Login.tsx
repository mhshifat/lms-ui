import React, { Fragment } from "react";
import { AuthContainer } from "../containers";

const Login = () => {
  return (
    <Fragment>
      <AuthContainer />
    </Fragment>
  );
};

export default Login;
